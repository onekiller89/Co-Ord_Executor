# MegaMind Hermes context

Scope: this is a personal WSL project. Treat existing uncommitted changes as owned by another active workflow. Inspect status and diffs before editing; do not reset, clean, rebase or overwrite them.

Boundaries:
- Keep MegaMind as the source owner for capture, extraction and Discord Forum publishing during the Hermes evaluation.
- A Discord emoji/reaction may create or update a pending intake record only. It must never directly execute code, create a branch, open a pull request, send a message, merge, deploy or change credentials.
- An implementation requires a separate explicit approval that identifies the executor, repository/worktree, branch and allowed scope.
- Do not read, copy, log or migrate secrets, OAuth data, session stores or `.env` content. Use documented environment-variable names only.
- Do not use personal integrations or OpenRouter/Qwen/DeepSeek routes for Defence, Leidos, customer or government-controlled material.

Engineering:
- Work in a dedicated worktree for approved code changes.
- Make the smallest viable change, add or update focused tests, and report the exact verification result.
- Preserve source identifiers, use idempotency keys for intake events, and leave an auditable status transition.
- Never send Discord, GitHub or other external writes without the user’s explicit approval.
